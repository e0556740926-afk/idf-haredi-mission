# Duet — הוראות עבודה לפרויקט

אפליקציית ניהול כספי משק בית לזוגות. עברית, RTL, mobile-first.
**קרא את `docs/00-product.md` לפני כל משימה.** אם משהו כאן סותר בקשה בצ'אט — עצור ושאל.

## החוק העליון של הפרויקט

המוצר הזה הוא הבטחה אחת: **אפשר לנהל בית ביחד בלי לוותר על פרטיות.**
לכל חשבון ולכל עסקה יש `owner_member_id` ו-`visibility` באחת משלוש רמות:

| רמה | מה בן/בת הזוג רואים | נספר בחשבון הבית? |
|---|---|---|
| `shared` | כל שורה | כן |
| `summary_only` | **סכום מצטבר בלבד** — בלי בית עסק, בלי תיאור, בלי קטגוריה | **כן** |
| `private` | רק שהחשבון קיים | **לא** |

**דליפה של שורה אחת מ-`summary_only` היא באג חמור (P0), לא באג עיצוב.**

### שלושה כללים שאסור להפר לעולם

1. **הלקוח לעולם לא קורא מטבלאות בסיס.** הרשאות `select` על `transactions`, `accounts` וכל טבלה שמכילה כסף — מבוטלות ל-`authenticated`. הגישה היחידה היא דרך `v_*` views ו-`rpc_*` functions. אם צריך נתון חדש — מוסיפים view או RPC, **לא** נותנים grant.
2. **RLS דלוק על כל טבלה** שיש בה `household_id`. ה-RLS הוא ההגנה האחרונה, לא היחידה.
3. **כל שאילתה מקבלת `viewer_member_id`**, לא `household_id`. אין מושג של "לקרוא את הנתונים של הבית" — רק "לקרוא את מה שהצופה הזה רשאי לראות".

## עוד כללים מחייבים

- **SQL מחשב, AI מנסח.** שום סכום, יתרה, תחזית או אחוז לא מחושב במודל שפה ולא בג'אווהסקריפט בקליינט. הכול ב-SQL או ב-Edge Function, ומוחזר מוכן.
- **אין השוואה בין בני הזוג.** אין endpoint, view, מסך או התראה שמציגים "מי הוציא יותר". אם בקשה מובילה לזה — סרב והסבר.
- **ברירת מחדל סגורה.** חשבון חדש נוצר כ-`private` עד שהבעלים משתף במפורש.
- **אין סודות בנקאיים.** לא שומרים שם משתמש/סיסמה לבנק. אף פעם. בשום טבלה.
- **מספרי חשבון וכרטיס** — 4 ספרות אחרונות בלבד, בעמודה `masked_number`.
- **כסף = `numeric(14,2)`**, לעולם לא `float`. מטבע תמיד נשמר לצד הסכום.
- **תאריכים** — `date` לתאריך עסקי, `timestamptz` לאירועי מערכת. אזור זמן של משק הבית נשמר ב-`households.timezone`.

## סטאק

- **Frontend:** Vite + React 19 + TypeScript + Tailwind. SPA, PWA-ready. RTL מלא.
- **Backend:** בשלב הזה — אין. נתוני דמה ב-`src/data/fixtures/`. בשלב הבא: Supabase — Postgres + Auth + RLS + Edge Functions.
- **State:** TanStack Query. אין Redux.
- **מיגרציות:** יגיעו בשלב חיבור מסד הנתונים. כרגע אין.
- **בדיקות:** Vitest ליחידה, pgTAP או בדיקות SQL ב-Vitest לשכבת הנתונים, Playwright ל-E2E.
- **פריסה:** Netlify לפרונט, Supabase לענן.

## שלב הפרויקט: פרונט מלא על נתוני דמה

אנחנו בונים כרגע את **כל הפרונט-אנד**, מקצה לקצה, מעל נתוני דמה. **אין עדיין Supabase ואין מסד נתונים.**
החיבור למסד נתונים מגיע בשלב נפרד אחריו, ולכן:

- **כל גישה לנתונים עוברת דרך `src/data/`.** ראה `docs/07-data-contract.md` — זה הקובץ הכי חשוב בשלב הזה.
- אף רכיב ואף מסך לא מייבא מ-`src/data/fixtures/` ישירות. אף פעם.
- כל פונקציית נתונים מקבלת `viewerId` כארגומנט ראשון.
- **סינון הנראות קורה כבר עכשיו**, בתוך `src/data/`. מסך לעולם לא מקבל שורה שאסור לו לראות, גם בשלב הדמה.

בזכות זה, חיבור מסד הנתונים בהמשך הוא החלפה של תיקייה אחת — לא כתיבה מחדש של מסכים.

## איך לעבוד כאן

1. **בנייה רצופה.** בשלב הזה אתה בונה את כל הפרונט בריצה אחת לפי `PROMPT-FULL-BUILD.md`, בלי לעצור לאישור בין שלבים. עוצרים רק אם חסר מידע שאי אפשר להשלים, או אם משהו בתיעוד סותר את עצמו.
2. **סדר בנייה יש, גם בלי עצירות:** טוקנים ורכיבי יסוד ← שכבת הנתונים והפיקסצ׳רים ← ניווט ופריסה ← מסכים לפי הסדר ב-`PROMPT-FULL-BUILD.md` ← מצבי קצה ← בדיקות.
3. **בדיקות חוזה הנתונים הן חובה** ורצות ירוק לפני שמכריזים על סיום.
4. **commit קטן ומתאר** בכל מסך שמסתיים.
5. **אל תמציא נתונים.** ה-seed ב-`docs/05-seed-data.md` הוא מחייב. מספרים אחרים — אל תמציא, שאל.
6. **אל תוסיף ספריות** שלא כתובות כאן בלי לשאול. במיוחד לא ספריית UI, לא ORM ולא ספריית גרפים.
7. **בסוף — דו״ח**: מה נבנה, אילו החלטות קיבלת שלא היו בתיעוד, מה חסר, ומה צריך לקרות בשלב חיבור מסד הנתונים.

## פקודות

```bash
npm run dev          # פיתוח
npm run build        # בנייה
npm run lint         # eslint + tsc
npm run test         # יחידה + שכבת נתונים
npm run test:data    # בדיקות חוזה הנתונים — חייב לעבור לפני כל commit
npm run e2e          # Playwright
```

## שפה וממשק

- כל טקסט למשתמש בעברית. **אין מחרוזות קשיחות בקוד** — הכול דרך `src/i18n/he.ts`.
- RTL מלא, כולל גרפים. הגרפים נכתבים ב-SVG ידני, בלי ספרייה.
- מספרים: `Intl.NumberFormat('he-IL')` + `tabular-nums`.
- אין אימוג'ים בממשק. אין אדום על הוצאות — אדום רק לשגיאת מערכת.
