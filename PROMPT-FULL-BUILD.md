# ההודעה ל-Claude Code

ריפו ריק → פרוס את כל הקבצים מהחבילה → `git init` → הדבק את כל מה שמתחת לקו.
קובץ העיצוב כבר בפנים, ב-`design/prototype.html`.

---

שלום. אתה בונה פרויקט בשם **Duet** — אפליקציית ניהול כספי משק בית לזוגות. עברית, RTL, mobile-first.

יש בריפו **פרוטוטייפ עיצוב מלא ועובד**: `design/prototype.html`. הוא HTML יחיד עם חמישה מסכים אמיתיים, טוקנים, רכיבים, אנימציות, מודאלים וטוסט. **הוא החוזה החזותי של הפרויקט.**

## שלב א׳ — קרא, בסדר הזה

1. `CLAUDE.md` — חוקי הפרויקט
2. `docs/00-product.md` — מה המוצר ולמה
3. **`design/prototype.html`** — פתח אותו בדפדפן, ואז קרא את הקוד. שים לב שהוא מכיל גופן מוטמע ב-base64 — דלג עליו, כל השאר קריא
4. **`docs/08-design-system.md`** — הטוקנים והרכיבים כבר חולצו מהפרוטוטייפ לכאן
5. **`docs/07-data-contract.md`** — הקובץ החשוב ביותר בשלב הזה
6. `docs/05-seed-data.md` — הנתונים, מחייבים
7. `docs/04-conventions.md` — מבנה ושמות

## המשימה

בנה את **כל הפרונט-אנד מקצה לקצה, בריצה אחת, בלי לעצור לאישור.**
אין Supabase, אין מסד נתונים, אין אימות אמיתי — הכול מעל `src/data/` עם פיקסצ׳רים.
עצור רק אם חסר מידע שאי אפשר להשלים. בכל מקרה אחר — החלט, רשום את ההחלטה, והמשך.

## שתי נקודות שחייבות להיות ברורות לפני שאתה כותב שורה

**1. הפרוטוטייפ הוא עמוד תצוגה, לא המוצר.**
בפרוטוטייפ יש מסגרות טלפון 390×844 על קנבס אפור, ולשוניות עליונות שמחליפות מסך. **זה אמצעי הדגמה. אל תבנה את זה לתוך המוצר.** במוצר יש ניתוב אמיתי, ניווט תחתון, והאפליקציה תופסת את כל המסך.
חריג אחד: שמור נתיב `/canvas` **ל-dev בלבד** שמציג את כל המסכים זה לצד זה כמו בפרוטוטייפ — נוח להשוואה מול המקור.

**2. מתג נקודת המבט משנה תפקיד.**
בפרוטוטייפ יש בתוך מסך הפעילות מתג "התצוגה של דנה / התצוגה של יואב". זה סיפור, לא פיצ'ר: במוצר האמיתי דנה פשוט רואה את התצוגה של דנה.
לכן: המתג הופך ל**מתג משתמש גלובלי בכותרת, ב-dev בלבד**, שמחליף את `viewerId` בכל האפליקציה. מסך הפעילות מרנדר תמיד את התצוגה של המשתמש הנוכחי.
**שורת "יואב רואה: הכול / רק כסכום" בכל עסקה — נשארת.** היא לא אמצעי הדגמה, היא לב המוצר.

הדבר הזה — שלכל עסקה יש בעלים ורמת נראות, ושרמת הביניים `summary_only` מציגה לצד השני **סכום בלבד בעוד הסכום כן נגרע מחישוב הבית** — הוא כל המוצר. **הסינון קורה ב-`src/data/`, לא ברכיבים.** רכיב לא מקבל שורה שאסור לו לראות ואז מחליט לא להציג — הוא פשוט לא מקבל אותה.

## סדר הבנייה

**1. יסודות**
Vite + React 19 + TS + Tailwind · RTL מלא · טוקנים מ-`docs/08-design-system.md` ל-`src/styles/tokens.css` (כולל הצבעים שעדיין אינם טוקנים — הפוך גם אותם) · **Rubik מ-Google Fonts, לא base64** · `src/i18n/he.ts` · `src/lib/money.ts` · `src/lib/date.ts`

**2. שכבת הנתונים**
`src/data/types.ts` · `src/data/fixtures/` לפי `docs/05-seed-data.md` והנתונים שבפרוטוטייפ · `src/data/mock/*.ts` שמממש את כל הפונקציות מ-`docs/07-data-contract.md` **כולל כל כללי הסינון** · hooks עם TanStack Query
ואז **`tests/data-contract/` — כל שמונה הבדיקות. חייבות לעבור לפני שממשיכים למסכים.**

**3. רכיבי יסוד — מהפרוטוטייפ, 1:1**
`VisibilityBadge` · `Money` · `Card` · `Alert` · `AggregateCard` · `PrivacyCard` · `FairnessCard` · `WinCard` · `ProgressBar` · `ProgressRing` · `Switch` · `Avatars` · `TxIcon` + סט האייקונים (העתק את אובייקט `icons` מהפרוטוטייפ כמו שהוא) · `BottomNav` · `Modal` · `Toast` · `PageHead` · `SectionHead` · `StepHead` · `ForecastChart`

**4. חמשת המסכים מהפרוטוטייפ — העברה נאמנה**
היום · פעילות · הבית · טקס · תזרים
**ההעברה נאמנה למראה:** אותם מרווחים, אותם גדלים, אותם טקסטים, אותה התנהגות. כולל הטיימר (600 שניות), המודאלים, הטוסט, שמירת ההחלטות עם אחריות ותאריך, כפתור "סמנו שהועבר" עם ביטול, והעתקת נוסח המייל ללוח.
**בגרף התזרים: ציר הזמן מתקדם מימין לשמאל** — זו בחירה מכוונת ל-RTL, שמר אותה.

**5. המסכים החסרים — על אותה שפה בדיוק**
הפרוטוטייפ לא כולל אותם. בנה אותם **רק מרכיבים שכבר קיימים**:

1. **אונבורדינג** — ארבעה שלבים: בחירת ארכיטיפ (קופה אחת / שלוש קופות / הפרדה מלאה / פער הכנסות) ← הזמנת שותף ← חיבור חשבונות (מדומה, עם התקדמות) ← הגדרת כיס אישי. הבחירה הראשונה משנה ברירות מחדל של נראות.
2. **חשבונות ומרכז בקרת נראות** — רשימת החשבונות עם סטטוס סנכרון, ולכל חשבון מתג שלוש-מצבי **עם תצוגה מקדימה חיה של מה בן/בת הזוג יראו**. זה המסך שמוכיח שהמודל מובן. הוספת חשבון ידני וייבוא CSV — ממשק בלבד.
3. **קבועות ומנויים** — לוח חודשי של חיובים, סך התחייבות חודשית, היסטוריית מחיר לכל סדרה (חדר כושר 189→231, נטפליקס 44.90→54.90).
4. **חיסכון ותובנות** — פיד תובנות ממוין לפי השפעה שנתית, ומונה חיסכון מאומת (1,428 ₪ בשנה).
5. **הגדרות הבית** — חברים, ארכיטיפ, כלל הוגנות, כיסים, כרית ביטחון, מצב שקט (שבת), ו**יומן נראות** שמראה מתי מי שינה רמת נראות — בלי לחשוף תוכן.

**6. מצבי קצה** — לכל מסך: טעינה (שלד), ריק, שגיאה, חיבור שבור עם באנר "עודכן לאחרונה ב־", והסכמה שפגה.

**7. סיום** — `npm run lint` · `npm run test` · `npm run test:data` · `npm run build` ירוקים.

## כללים

- **הפרוטוטייפ מנצח במראה. התיעוד מנצח בהתנהגות ובנתונים.** סתירה מהותית — רשום בדו״ח, אל תעצור.
- אין מחרוזת עברית בתוך JSX — הכול דרך `src/i18n/he.ts`.
- אין חישוב כספי ב-TypeScript. סכומים מגיעים מוכנים מ-`src/data/`.
- אין ספריית UI, אין ORM, אין ספריית גרפים. SVG ידני.
- אין אימוג'ים. אין אדום על הוצאות — אדום רק לשגיאת מערכת.
- אין בשום מסך השוואה בין שני בני הזוג.

## מה לא לבנות

Supabase · מיגרציות · אימות אמיתי · חיבור לבנק · AI וצ'אט · אוטומציות ו-WhatsApp · תשלומים ומנויים · שווקים נוספים.
נדרש משהו מהם — `TODO` מתועד + שורה בדו״ח.

## דו״ח הסיום — `REPORT.md`

1. מה נבנה — מסכים ורכיבים
2. **החלטות שקיבלת שלא היו בתיעוד** — הכי חשוב
3. פערים בין הפרוטוטייפ למימוש, ולמה
4. מה לא נבנה ולמה
5. **רשימת המקומות שיושפעו בשלב חיבור מסד הנתונים** — אמורה להיות קצרה מאוד. אם היא ארוכה, משהו דלף מחוץ ל-`src/data/`

התחל.
